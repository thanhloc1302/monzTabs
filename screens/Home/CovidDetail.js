import React from "react";
import { View, StyleSheet } from "react-native";

export default function CovidDetail({ covid_vn, covid_world }) {
  return <View></View>;
}

const styles = StyleSheet.create({
  containercarousel: {},
});
